项目结构
    crud：封装的数据 库增删改查的代码
    models： 模型类
    routers：拆分出来的路由
    schemas：pydantic数据校验
    utils：工具
    config：数据库相关，缓存配置配置相关
模块化路由
    模块化路由就是把每个业务功能的接口拆分到独立的文件里，在统一挂载到主应用中
接口实现流程
    模块化路由 -> API 接口规范文档
        定义 APIRouter 实例
        注册路由 include_router()
        参照接口规范文档
    定义模型类 -> 数据库表（数据库设计文档）
        参照数据库表
    数据库 CRUD -> 在 crud 文件夹里面创建文件，封装操作数据库的方法
        select(模型类)
        add()
        update()
        delete()
    路由调用逻辑 -> 在路由处理函数里面 crud 封装好的方法，响应结果
        Depends 注入数据库依赖
        调用逻辑
        响应结果